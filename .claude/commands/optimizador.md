Carga la skill del área correspondiente según el archivo o funcionalidad indicada.

Actúa como experto en optimización de aplicaciones Next.js + Supabase.

Analiza y optimiza: $ARGUMENTS

Foco en:
1. Queries Supabase: ¿Se pueden combinar? ¿Falta paginación? ¿`select('*')` donde debería ser selectivo?
2. Rerenders de React: ¿Faltan useMemo/useCallback? ¿Estados que se pueden unificar?
3. Componentes grandes: ¿Se puede dividir sin crear abstracción prematura?
4. Código duplicado: ¿Candidatos a extraer a `src/components/` o `src/lib/`?

Solo propón cambios con beneficio real. No optimices lo que ya es suficientemente bueno.
No añadas complejidad para casos hipotéticos futuros.
Al terminar, actualiza la skill correspondiente si el patrón optimizado es reutilizable.
